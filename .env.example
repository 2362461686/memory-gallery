# 会话签名密钥(必填)。缺失或短于 32 字符,应用会拒绝启动。
# 生成:openssl rand -base64 32
AUTH_SECRET=

# DeepSeek API Key(选填)。不配也能用 —— 装订会自动降级为按日期起名,
# 并且会在界面上明确告诉用户"没有配置 AI 密钥",不做静默降级。
DEEPSEEK_API_KEY=

# 站点正式地址(部署时填)。分享链接的预览卡片靠它生成绝对 URL,
# 不填会回落到 localhost,微信等渠道的预览会失效。
NEXT_PUBLIC_SITE_URL=
