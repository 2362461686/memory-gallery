# 项目进度与交接

> 更新:2026-08-31 · 分支 `fix/import-upload` · 最新 `b6a4969`+
> **新会话开工前读这一份就够了。** 想改界面,再读 `.claude/skills/`(见下)。

---

## 一、这是什么

**漫画风回忆录。** 上传照片 → 按上传批次自动分「话」→ 排成漫画分镜 → 装订成一卷可分享的回忆集。

设计命题(一切决定的源头):**把平凡的日子变得不平凡。**
判据:用户看到自己那些普通照片被这样呈现之后,会不会觉得「原来我这几天过得还挺像回事的」?

**跑起来**(见 README):`npm ci` → `cp .env.example .env` → 生成 `AUTH_SECRET` → `npm run dev`。
缺 `AUTH_SECRET` 会拒绝启动,这是有意的安全设计不是 bug。

---

## 二、⛔ 已经定死的决定(别重新提案)

| 决定 | 原因 |
|---|---|
| **不做 QQ 空间 / 微信导入** | 接口非官方、防盗链、Cookie 随时失效,不值得用户折腾。代码已全删 |
| **不做 3D 画廊** | 曾用 three.js,被判定「粗糙、廉价感」,已删,依赖也已卸 |
| **视觉方向 = 黑白漫画 / 单行本** | 不是彩色动漫立绘,不是扁平插画 |
| **界面要动态** | 原先定过「印刷品不会自己动」,已被推翻。现在是「一部动起来的漫画」 |
| **不让用户交代产品该自己算的信息** | 补录表单只能是可选折叠项,不能必填 |

---

## 三、人物素材那条已收口（2026-08-31）

**结论:不用人物素材,漫画感由版面语法提供。别再找素材了。**

### ⛔ 少年 Jump 角色不能用

周令杰提过「碧蓝之海 / 死神 / 航海王 / 火影,不商用所以没问题」。**不成立**:
仓库是公开的,放进去属于向公众提供(信息网络传播权),和商不商用无关;
集英社在 GitHub 实际发过 DMCA(`github/dmca` 2023-03-22)。风险是仓库下架 + 账号 strike。

### 免费素材:再查一轮,黑白漫画风人物依然不存在

| 来源 | 结论 |
|---|---|
| Linustock | ❌ 条款禁止原样再分发 |
| いらすとや | ❌ 商用有数量上限,风格也不符 |
| Open Peeps / Open Doodles / Fresh Folk / Highlights(CC0) | ⚠️ 授权干净,但全是圆润手绘或扁平插画,不是漫画线稿 |
| **《北斎漫画》(Smithsonian,CC0)** | ⚠️ **授权最干净,已实测能下高清**;十五卷全是「平凡人做平凡事」的墨线小人,跟本项目命题很搭 —— **但是江户人,配现代手机照片气质打架,已否** |

### ⛔ 手绘剪影:第二次证伪,别再试

`Silhouette.tsx`(128 行,`1b89153` 那次 revert 的幸存文件)从没被渲染过。
08-31 抠出来单独渲染,五个全是**正面半身的通用头像 blob** ——
姿势读不出动作、白高光细得像划痕、走路的腿是两根断棍。已判定不过关。

**加上上一次手写 SVG 画脸的失败,这是第二次证明:我画不出漫画人物。别再安排这件事。**
(`Silhouette.tsx` 保留未删,但**首页不接它**;要删随时可删。)

## 三之二、于是走版面语法（已落地）

让人认出「少年漫画」的其实不是角色,是三样印刷语汇,**都不需要素材**:

| 语法 | 落在哪 | 实现 |
|---|---|---|
| **ベタ**(整格实心黑) | 第 3 格「回家路上」 | `.panel-beta`:底恒为 `--ink`、线稿转 `--paper`、旁白框仍是白框黑字 |
| **決めゴマ**(集中線收束) | 第 5 格「没发出去的那条消息」 | `focus-lines` + 新增 `.fx-focus-strong` |
| **オノマトペ 放大** | 決めゴマ的「叮」 | 中空字,`sm:text-6xl` |

⛔ **出血(负边距顶到页边)在这一页做不了** —— 这页的「纸边」就是刊头那道框,
负边距把框撞断,看着像渲染坏了。试过,已撤,别再提案。

⚠️ **ベタ格的拟声词必须单独配色**:那一格底色恒等于 `--ink`,沿用默认的 `--ink` 描边
等于黑描黑;夜刊下这格还会翻成浅色。已用 `.panel-beta .manga-sfx` 钉死为
黄字 + `--paper` 描边,两个主题都实拍验过。

⚠️ **`.sfx-land` 已被 `.sfx-drop` 取代**:落地动画只做缩放不做旋转,
旋转留给内层 `.manga-sfx`。原因是两个 `animation` 挂在同一元素会互相覆盖 ——
決めゴマ的持续抖动 `.fx-sfx-shake` 必须挂在内层才不被落地动画吃掉。

## 四、界面设计:改之前必读

`.claude/skills/` 下有两份,分工见该目录的 `README.md`:

- **`frontend-design/`** —— Anthropic 官方(MIT,含 LICENSE)。通用方法论:先出方案(配色/字体/版式/**signature 记忆点**)→ 回头批判是否落入 AI 通用套路 → 才写代码。
- **`memoir-ui/`** —— 本项目专用。令牌表、既有语汇表、材质隐喻、动效判据、文案语气、交付前自检清单。

⚠️ **官方 skill 点名的 AI 默认套路 #1 是「暖奶油底 + 高对比衬线 + 赤陶色点缀」,而本项目配色正中这一条**(`--paper #fbf7ee` + Noto Serif 900 + `--accent #d6402b`)。豁免依据是"需求明确指定视觉方向时原话优先"(周令杰要漫画风,漫画印刷本来就是新闻纸底+墨线+专色)。**但这意味着配色本身不构成差异化,记忆点必须来自别处。**

### 已建成的视觉资产

- **令牌**:`--ink` `--paper` `--accent` `--sun` `--sky` `--on-bright`(黄底青底上的固定墨字,两个主题都不翻转)
- **字体(自托管)**:站酷庆科黄油体(标题)/ 马善政毛笔(手写)/ Noto Serif SC(正文)。
  ⚠️ 子集只含当前文案用到的 **621 个字**,`src/fonts/`。**改文案用了新字,那个字会掉回系统字体** —— 需要重新生成子集。
  ⚠️ 曾用 `next/font/google`,dev 模式拿不到代理导致首页 500,已改 `next/font/local`。别改回去。
- **真网点引擎**:`src/vendor/halftone-kit/`(halftone-ui,MIT,含 LICENSE)。Poisson-disk 蓝噪点云 + 阈值渲染,给 `field(u,v)->0..1` 出真印刷网点。包装在 `src/components/home/Halftone.tsx`。
- **首页 signature**:自我装订的漫画页 —— 五格平凡瞬间逐格砸下、线条自己描出、拟声词落地、整页一震。
  08-31 加进 ベタ格 与 決めゴマ(见第三之二节),那一格实心黑是整页唯一的视觉重音。

---

## 五、外部复核报告(权威待办来源)

两份都在 `/home/zhoulinjie/open-webui-dev/instance/qa-local-workspace/temporary/`:

1. `memory-gallery-product-audit-and-cc-backlog-2026-08-28.md` —— 工程视角,P0/P1/P2 分级
2. `memory-gallery-real-user-experience-review-2026-08-28.md` —— 真实用户体验视角

⛔ 报告结论:**别再堆漫画装饰**,阅读器已足以验证方向,收益低于修分享/移动端/数据安全。

### 已修

JWT 固定后备密钥 · 手机导航溢出 · 分享页断层(重写为同一个阅读器) · 装订半成功 · 上传只验扩展名(已加 magic bytes) · 旧紫色视觉与「策展」文案残留 · 设置静默截断 · 依赖高危(next 16.3.3 / sharp 0.35.4,audit 归零) · 记忆信息没进阅读页(日期/地点/原话) · 标题重复 · AI 静默降级 · 「0张」计数 · metadataBase · 装订预览页 `/bind` · 分享管控(撤销/有效期/密码/换链接) · **图片公开可下载(MG-P0-03,见下)**

### 未修(按建议顺序)

| ID | 问题 |
|---|---|
| **MG-P0-02** | JSON 文件存储无并发与事务保障 → 要迁 SQLite |
| P2 | 分享的 `allowDownload` 字段仍是摆设:图片一律 `Content-Disposition: inline`,没按它拦下载 |
| **P0** | 隐私与数据说明、导出、删除账户(**依赖上面两条**) |
| MG-P1-03 | 时间线按上传批次而非真实事件聚类 |
| MG-P1-04 | 无法编辑已装订的回忆集 |
| MG-P1-06 | AI 输出无 schema 校验 |
| MG-P1-09 | 无有效测试(`test_pages.py` 已失效,查的是删掉的旧文案) |
| P1 | 首页「继续阅读 / 待整理 / 最近新增」、搜索、年月导航 |
| **P1** | **首页 320px 版面坏的**(标题与 CTA 溢出分格、caption 截断成「加班那…」)。**改版面语法之前就是坏的**,08-31 没修 —— 五格挤在三列网格里每格只剩 ~85px,要真修得让窄屏换布局,不是调字号能解决 |
| P2 | `ThemeToggle` 水合不匹配(服务端渲月亮、客户端渲太阳),dev overlay 常驻报错。**改版面之前就有** |
| P2 | `Halftone.tsx`(halftone-ui 真网点引擎)**目前是死的**:`field` 是函数 prop,而 `page.tsx` 是 server component,函数传不进客户端组件。首页那个 import 已删。要用得先加一层 `"use client"` 包装 |

---

### MG-P0-03 已修:照片离开 public/,取图一律鉴权

照片以前落在 `public/uploads/`,Next 会无条件当静态资源发出去 —— 知道文件名就能下,
不用登录,也绕过分享的撤销/过期/密码。现在:

- 文件搬到 `data/uploads/`(不对外);库里 `mediaUrls` 仍存 `/uploads/<file>`,
  那是**逻辑地址**不是 URL,所以老库不用迁。渲染时经 `src/lib/media.ts` 的 `mediaSrc()` 翻译。
- 本人取图 → `/api/media/<file>`,判据是「这张图在他名下的 post 里」。
- 分享取图 → `/api/share/<token>/media/<file>`,先过同一道 `checkShareAccess`,
  再判「这张图是这本册子的成员」——分享链接只授权那一本,不能拿它翻作者别的照片。
- 查无此图和无权看此图**一律 404**,不用 403(403 等于告诉外人这个文件存在)。
- 老库兼容:文件还在 `public/uploads/` 的,首次访问自动搬走;批量搬用 `npm run migrate:uploads`。
- 分享密码 cookie 的 path 从 `/share/<token>` 改成 `/`,否则图片路由收不到,解了锁还是一页裂图。

⚠️ **改这块之前先明白**:`exhibitionHasMedia` 故意**不认 coverImage**,只认成员关系 ——
这台机器的 dev 库里就有封面指向册外照片的脏数据,给封面开后门等于开一个绕过成员判定的口子。

**验的方式**(dev server 实跑,不是只看类型):越权取别人的图 404、未登录 404、
路径穿越 404、撤销/过期/未解锁/错密码/换链接后取图全 404、对密码后 200、字节与源文件一致;
并做了**突变验证** —— 把两处判定分别注释掉,越权请求立刻变 200,还原后回到 404,
证明拦截确实是这两行在起作用,不是碰巧。

---

## 六、这台机器的坑(会浪费大量时间)

- ⚠️ **这是公司电脑,只用来改代码,不是运行环境。** 周令杰回自己电脑跑。
  **验证一律用静态检查**:`npx tsc --noEmit` + `npm run lint`。别死磕 `npm run dev` 起不起得来。
- ⚠️ **`node_modules` 已被多次中断的安装搞坏**,`node_modules/.bin/next` 时常丢失。
  绕过办法:`node node_modules/next/dist/bin/next build`。
  但 `package.json` / `package-lock.json` 是干净的,别人 `npm ci` 装出来没问题。
- ⚠️ **系统级代理,npm 只有约 6.5 KB/s**。周令杰已明确**不允许直连**,别再提。
  用 `--registry=https://registry.npmmirror.com --fetch-timeout=600000 --fetch-retries=5`,后台跑。
- ⚠️ ⛔ **别自己写 `until ! pgrep -f "xxx"` 轮询等后台任务** —— `pgrep -f` 会匹配到等待脚本自身,
  曾因此空转 24 分钟。用 harness 的 `run_in_background`,跑完自动通知。

---

## 七、Git 纪律

- ⛔ **commit 邮箱必须是 `2362461686@qq.com`**。仓库级已配好,但曾配错过(用成 outlook),16 个提交被迫 filter-branch 重写。新克隆记得先 `git config user.email`。
- ⛔ **绝不加 `Co-Authored-By: Claude` 尾行** —— 会让 Contributors 显示成 Claude 而不是本人,踩过一次。

**测试账号**:`demo@memory.test` / `demo1234`(含横竖图两话 + 三种长度文字 + 自定义页边吐槽)
